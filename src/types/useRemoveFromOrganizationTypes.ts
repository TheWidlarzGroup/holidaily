export type RemoveFromOrganizationTypes = {
  userId: string
}

export type RemoveFromOrganizationDataTypes = {
  user: {
    confirmed: boolean
    email: string
    firstName: string
    lastName: string
    id: string
    occupation: string
    role: string
  }
}
